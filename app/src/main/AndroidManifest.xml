<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.travelapp">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ActionBar"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup"
        tools:targetApi="m">
        <activity 
            android:label="Update Tour"
            android:name=".UpdateTourActivity"
            android:parentActivityName=".ui.MainActivity"/>
        <!-- Stop points -->
        <activity
            android:name=".ui.MapsActivity"
            android:parentActivityName=".ui.MainActivity" /> <!-- Main -->
        <activity android:name=".ui.MainActivity" /> <!-- Facebook Login -->
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" /> <!-- Facebook Modal Login -->
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity> <!-- Verify email, phone -->
        <activity android:name=".ui.VerifyActivity" /> <!-- Reset password -->
        <activity
            android:name=".ui.PasswordRecovery"
            android:parentActivityName=".ui.LoginActivity" /> <!-- Register -->
        <activity
            android:name=".ui.RegisterActivity"
            android:parentActivityName=".ui.LoginActivity" /> <!-- Login -->
        <activity android:name=".ui.LoginActivity" /> <!-- Splash -->
        <activity
            android:name=".ui.SplashActivity"

            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Google API key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" /> <!-- Facebook App id -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>